# K均交易法 — 程序化交易实施备忘

来源：本目录下 `K1.docx` 与 `K2.docx`（作者：笑傲茶湖段）。两本书均为 100% 图片扫描页（K1 共 591 页 / K2 共 1584 页），无可提取文本，本文为视觉抽样阅读后的核心策略与注意事项的整合。

---

## 一、方法论总览

K 均交易法只看两样东西：**K 线** 和 **均线**。一切其它指标（MACD、KDJ、基本面、消息面、价值评估）都不参考，因为它们落后于价格本身——价格变了别人才发分析报告。

作者把交易系统分为三代：

| 代次 | 看什么 | 适用场景 |
|------|--------|----------|
| 一代系统 | 只看 K 线（高低点 + 颈线突破） | 入门，30 分钟及以上周期 |
| 二代系统 | 只看均线（小均线对比大均线） | 走势判定 |
| 三代系统 | K 线 + 均线相互印证 | 作者主力，赚钱最多 |

**核心交易哲学**

- **天道无常，按分类概率办事**：单笔不必赢，长期靠概率优势。作者声称 6 成胜率即可稳定盈利，70% 的单子是大赚，其余是小亏小赢。
- **机会出现就进场，机会消失就离场**：不预测，只识别。
- **等待是主业**：等待自己交易系统发出信号，不是等灵感。在价格高低点出现以前，永远无法知道，这就是交易的真谛。
- **进出点是一个区域，不是一个点位**：不要死扣某个具体价格。

---

## 二、价格波动规律（道氏理论的可执行翻译）

K 均交易法的高低点判定完全建立在道氏理论的三种走势定义之上：

- **上涨**：上升浪能连续不断地突破前期的高点，随后的回撤的低点能在前期的低点之上结束。简记：**新高之后是不新低**。
- **下跌**：反之。简记：**新低之前是不新高**。
- **盘整**：既不创新高也不创新低，或者既创新高又创新低（震荡假突破）。

派生规则：

- **创新高 / 创新低 → 趋势控制力依然存在**。
- **不创新低 + 新高 → 做多；反之做空**。这是最简单也最重要的两组排列关系。
- **小均线 > 大均线 → 大概率上涨**；反之大概率下跌。均线代表市场平均成本，K 线对均线的排列即趋势分类。

---

## 三、5 步交易系统（K1 第三章核心）

逐条都需要在程序里固化下来：

| 步骤 | 内容 | 程序化要点 |
|------|------|-----------|
| ① 固定操作级别 | 选定一个固定的 K 线周期 | 不在多周期之间漂移，避免主观切换 |
| ② 固定操作线 | 选定一条主均线（默认 **MA150**） | 与该周期匹配，确定后不动 |
| ③ 会画高低点 | 按道氏定义滚动维护当前高低点 | 实现一个 swing-high / swing-low 检测器 |
| ④ 确认突破标准 | 用 **颈线**（前高 / 前低水平线）作为突破阈值 | 突破颈线 = 信号触发，区分真假突破 |
| ⑤ 严格执行 | **到位就动，不到位就不动** | 信号触发即下单，无信号绝不动作 |

**额外细节**

- **加仓只能加一次**，且必须在突破方向被进一步确认后；浮亏加仓属赌徒思维，禁止。
- 单笔风险控制好的前提下，"加仓只能加一次"对应程序里的 `pyramid_count <= 1`。
- 入场后让利润奔跑，亏损迅速截断（顶级原则：迅速止损）。

---

## 四、三级别联立（K2 的进阶）

K2 在 K1 之上加了**三条均线 + 三种周期**的多级别协同。

### 4.1 三均线模型

软件标注为 `MA组合(0, 30, 750, 150, 0, 0)`，即同时挂三条均线：

| 角色 | 默认参数 | 颜色（书中例图） | 含义 |
|------|---------|-------------------|------|
| 大线 | **MA750** | 紫色 | 长期趋势过滤，相当于"老板说话" |
| 操作线 | **MA150** | 绿色 | 主操作均线，决定多空方向（K1 同款） |
| 小线 | **MA30** | 黄色 | 进出场细化、止损位参考 |

任意级别均线都对应一组高低点：黄色横线 = MA30 前高，绿色横线 = MA150 前高，依此类推；这就是"不同级别的高低点同时存在"。

### 4.2 周期划分

- **大周期**：1 小时、2 小时（超过 2 小时为"超大周期"，不推荐）。
- **30 分钟**：分界线，不大不小。
- **小周期**：15 / 10 / 5 / 3 / 1 分钟以及秒级。作者偏好**小周期**。

K2 上中下三篇分别对应：

- **上篇 — 中长线**（用 1H / 2H K 线 + MA(30,150,750)）
- **中篇 — 短线**
- **下篇 — 日内**

### 4.3 联立级别

- **单级别**：只看一个周期的一组均线。
- **两级别联立**：用大周期定方向 + 小周期定入场。
- **三级别联立**：长 / 中 / 短三周期同向，胜率最高。

操作时第一件事是明确：**这一笔做的是单级别、两级别联立、还是三级别联立？** 不同级别有不同分类规则，分别执行即可，不要把多个级别混在一起做精细化纠结。

### 4.4 突破与双突破

- **突破**：K 线突破前高/前低颈线。
- **双突破**：K 线 + 均线（操作线）同时被突破，更可靠的信号。
- 出现明显突破（单根光头光脚或近似）即翻向，例如下跌中明显突破上沿 → 翻空 / 翻多视方向而定。

---

## 五、资金管理

- **固定手数法**：在一个固定考核周期内（如月、季）固定每次开仓手数。
  - 例：1 万元起步且每次 1 手。账户到 2 万 → 改为 2 手。账户到 4 万 → 4 手。线性放大，不复利豪赌。
  - 回撤阶段（账户从 4 万跌回 2 万）→ 立即降回 2 手。**亏损时优先降手数，保住交易资格**。
- **直线加速规划（保守 200%/年）**：5 万本金五年目标 160 万，10 万本金五年目标 320 万。这是作者自己给的"心理锚定"，不是承诺。
- 单笔风险必须在系统层面截死，禁止扛单不止损（"鳄鱼法则"）。

---

## 六、心法与执行（容易被忽视但决定盈亏）

1. **悟性 → 心态 → 执行 → 结果**：成功交易是 信息→思考判断→行动→盈亏 的闭环；其中信息错（看错图、听错消息）和判断错占绝大多数亏损来源。
2. **不要追求精确度，先有节奏**：节奏（趋势）大过精确度。无节奏，无精确度。
3. **拒绝交叉验证多套体系**：找到一套有效的，长期一致地执行下去。
4. **反复盘整、假突破、震荡是必经之苦**。出现盘整不要在中段乱开仓，等下一次突破出现再做。
5. **不要频繁换品种**：选一个做下去；好坏都是概率，频换会把概率优势抽掉。建议**周期级别地换品种**（数月以上）而非天级别。
6. **空仓是状态，不是失败**：没有信号时，"什么都不做"本身就是优秀的交易。
7. **分类穷尽**：把所有 K 线 + 均线的相对位置组合（"X 仪 X 象 X 卦"式分类）穷举一遍并对应规则。书中反复强调，能完全分类才能机械执行。

---

## 七、对程序化实现的注意事项

将上述策略落到代码时，建议固化以下要点：

1. **可配置参数集合**（默认值取自书中 K2 模型）：
   - `period`: 1h / 2h / 30m / 15m / 5m
   - `ma_big = 750`, `ma_main = 150`, `ma_small = 30`（参数三选一同时启用，对应单/二/三级别联立）
   - `swing_lookback`: 用于识别 swing-high / swing-low 的回看根数
   - `breakout_buffer`: 颈线突破的最小幅度（避免假突破，用 ATR 或固定 bp）
   - `stop_loss_atr`: 单笔止损以 ATR 倍数为单位
   - `pyramid_count <= 1`：每个方向最多加仓一次
   - `position_units`: 当前手数；按账户权益分档调整

2. **状态机设计**：
   - `STATE`: `FLAT` / `LONG` / `SHORT`
   - `TREND`: `UP` / `DOWN` / `RANGING`（按"是否创新高 / 是否不创新低"分类）
   - `SIGNAL`: `BREAKOUT_UP` / `BREAKOUT_DOWN` / `DOUBLE_BREAKOUT_UP` / `DOUBLE_BREAKOUT_DOWN` / `NONE`
   - 切换条件全部以 K 线收盘为准（避免盘中假突破触发），这是程序化最稳妥的离散化点。

3. **多级别一致性检查**：仅当大 / 中 / 小三周期 `TREND` 同向时下三级别联立单；否则降级为两级别或单级别仓位。

4. **回测与实盘必须用同一份高低点定义**：把"创新高 / 不创新低"的判定函数单独抽出来做单元测试，因为它是整个策略的真值源头。

5. **禁项白名单**（程序里硬拒）：
   - 不在 `RANGING` 状态盘整中段开仓（必须等下一次颈线突破）。
   - 不浮亏加仓。
   - 不跨品种快速切换（设置最小持有周期）。
   - 信号未到不开仓（允许长时间空仓）。

6. **执行前自检清单**（每次发单前打印）：
   - 当前操作级别是？操作线是哪条？
   - 颈线在哪（前高 / 前低数值）？
   - 是否真突破（收盘价 vs 颈线 + buffer）？
   - 是否多级别同向？
   - 止损位 / 头寸单位是否合规？

---

## 八、文件来源与可信度声明

- 原书章节通过视觉 OCR **抽样**阅读得出（K1 约 30 页 / K2 约 30 页样本），未做逐页阅读；细节如某些图例的具体进出点位置可能有遗漏。
- 写入此文档的全部数值（MA150 / MA750 / MA30、200%/年、6 成胜率、加仓 1 次等）均来自书中明文。其余执行细节如"突破 buffer = 多少 ATR"等数值书中**未给出**，需要自己回测确定。
- 本文档不构成投资建议；任何参数都需要在历史数据上回测、压力测试、并以小资金验证后再上量。

---

## 九、各市场最佳实战配置（v1–v8 回测实证总结）

经过 8 代策略迭代（v1 → v8）+ 三套数据窗口 + 128 组合网格搜索（详见 `analysis.txt`）后，每个市场都有了清晰的"最优配方"。**最重要的发现：没有"通吃"的最优，只有"按目标 / 按市场"选**。

### 9.1 美股（US）

**首选：v4 + 分析师/动量综合筛选**（最高绝对收益 +677%）

| 项 | 配置值 |
|---|---|
| 策略版本 | v4（v3 + 多周期共振 + 市场宏观 master switch） |
| 数据 | 3 年日线 |
| 票池 | 从 S&P500 候选里按动量+共识打分 top 20 |
| 入场过滤 | MA30 > MA150 + 周线 MA10 > MA30 + S&P500 ADX > 18 + 颈线突破 |
| 止损 | 初始 2.2×ATR，浮盈 ≥ 1×ATR 后切 2.5×ATR 拖尾 |
| 加仓 | 浮盈 ≥ 1.5×ATR 加 1 仓 |
| 历史表现 | **+677%（59 票合计）/ PF 1.7 / 单笔 +0.80R** |

**次选：v8 + macd, obv, mfi, atr_exp, cci**（PF 最高 2.44，单笔精挑）

| 项 | 配置值 |
|---|---|
| 适用 | 风控优先、容忍交易频次低 |
| 入场额外过滤 | MACD 柱 > 0 + OBV > OBV 20 根 MA + MFI ∈ [50, 80] + ATR > 1.2×50 根均 ATR + CCI > +100 |
| 止损 | 1.5×ATR 初始 + 2.5×ATR 拖尾 + **10% 硬止损** |
| 历史表现 | **PF 2.44 / 仅 40 笔 / 美股部分 +300%**（少而精） |

**为什么美股是首要市场**：3 年趋势性强，所有策略都能赚钱；选 v4+screen 是因为多重过滤会"挤掉"票池筛选的增益（详见 §11.5）。

---

### 9.2 港股（HK）

**首选：v8 + atr_exp, kdj**（震荡市最优）

| 项 | 配置值 |
|---|---|
| 策略版本 | v8（v7 删除 BB + 加 MFI/CCI/KDJ + CN master） |
| 数据 | 3 年日线 |
| 票池 | 从 HSI/恒生综指里按动量+共识打分 top 20，或随机 20 |
| 入场过滤 | 标准 v4 全套 + ATR > 1.2×50 根均 ATR + KDJ K > 50 ∧ K > D ∧ J > D |
| 止损 | 初始 1.5×ATR，浮盈 ≥ 1×ATR 后切 2.0×ATR 拖尾 + 10% 硬止损 |
| 连亏冷却 | 连续 2 笔亏损 → 跳过 30 根 K 线 |
| 历史表现 | **+22% / PF 1.13 / 17 笔（高度选择性）** |

**为什么这套最适合港股**：
- 港股 3 年震荡时间多，**ATR 扩张过滤**只让"波动率刚扩张"时入场（突破刚发生），避免追涨末段
- **KDJ** 对震荡区内"短期超买/超卖"敏感，能识别真正的反转
- 笔数少（17 笔 / 3 年）但 PF 1.13 是港股所有变体里第一个 > 1 的

**避免**：v4 + screen 在港股上表现远不如美股（主要靠美股拉动 +677%）。港股要"等真正的趋势"，不是"博机会"。

---

### 9.3 A 股（CN）

**首选：v8 + atr_exp, kdj, mfi（含 CN 大熊期 master switch）**

| 项 | 配置值 |
|---|---|
| 策略版本 | v8 |
| 数据 | 3 年日线 |
| 票池 | 从 CSI300 候选里筛 top 20 |
| 入场过滤 | 标准 v4 全套 + ATR 扩张 + KDJ + MFI ∈ [50, 80] |
| **CN 大熊期 master switch** | **CSI300 close > MA200 才允许做多 / < MA200 才做空** ← 关键 |
| 止损 | 初始 1.2×ATR（最紧）+ 1.8×ATR 拖尾 + 10% 硬止损 |
| 加仓 | **关闭**（A 股震荡多，加仓亏更多） |
| 连亏冷却 | 连续 2 笔亏损 → 跳过 60 根 K 线 |
| 历史表现 | **−12% / PF 0.68 / 6 笔**（仍亏，但比 v1 的 −238% 减亏 95%） |

**坦白告诉你**：A 股 2023–2026 是结构性下跌期，**所有 8 套策略变体在 A 股上都是亏损的**。这不是策略失败，是市场本身就没有持续趋势。

**A 股的正确认识**：
1. 用 CN master switch 把"大熊期"屏蔽掉 → A 股 −238% 改善到 −12%
2. 当 CSI300 跌破 MA200 时，**不交易 A 股**，转去做美股 / 港股
3. 等待中国市场重回 CSI300 > MA200 + 趋势态恢复后再启用 A 股

**避免**：在 A 股上单独使用 v3/v4/v5/v6/v7 都有显著亏损（−105% 到 −406%）。

---

### 9.4 全市场综合（一刀切）配置

如果你只想用一套规则跑所有市场：

| 项 | 配置值 |
|---|---|
| 策略版本 | v8 |
| filters | `cci, kdj, macd, obv` |
| 数据 | 3 年日线 |
| 历史表现 | **+411% / PF 1.49 / 140 笔** |

**优点**：
- 统一规则，运维简单
- PF 1.49 仍是"系统性盈利"
- 包含 v8 的所有改进（10% 硬止损、CN master 等）

**缺点**：
- 比"按市场分开调"略低（v4+screen 美股 +677%）
- 比"美股专用 v8 PF 2.44"也略低

---

### 9.5 小时线交易（高频 / 短线）

**首选：v6 + macd, rsi**

| 项 | 配置值 |
|---|---|
| 策略版本 | v6 |
| 数据 | 730 天 1 小时 |
| filters | `macd, rsi` |
| 历史表现 | **+45% / PF 1.07**（小时线上唯一转正方案） |

**重要事实**：小时线上 v1/v2/v3 全部亏损（−274% ~ −580%），只有 v4 起步靠周线共振 + 市场 master switch 才转正。**小时线策略对多周期共振极度依赖**。

**避免**：
- v5 在小时线上把 RSI 反转加进去反而拖累 → v5 hourly 比 v4 hourly 更差
- v8 在小时线没有针对性优化，未实测

---

## 十、最优指标组合（v8 网格搜索结论）

基于 v6（8 组合）+ v7（32 组合）+ v8（128 组合）三轮网格搜索的实证排名：

### 10.1 经实证有效的指标 ✓

| 指标 | 用法 | 适用市场 | PF 增益 |
|------|------|---------|---------|
| **MA30 / MA150 / MA750** | 核心 stack（永不去掉） | 所有 | — |
| **MACD 柱 > 0** | 趋势确认 | 美股主要有用 | +0.05~0.10 |
| **OBV > OBV 20 根 MA** ⭐ | 量价双确认 | 美股 | +0.20 |
| **MFI 50–80 zone** ⭐ | 量能加权 RSI | 美股 | +0.30（PF 2.11→2.44）|
| **ATR > 1.2×50 根均 ATR** | 波动率扩张过滤 | 港股 / A 股 | +0.30（A股 −78%→−24%）|
| **KDJ 三线** ⭐ | 区间内超买超卖 | 港股 / A 股 | +0.13（CN PF +0.13）|
| **CCI > +100** | fine-tune | 美股 | +0.05 |
| **RSI 区间** | 仅小时线 | 小时线 | daily 反而损害 |
| **10% 硬止损** ✓ | 防灾难单笔 | 永远加上 | 单笔最差从 −2.55R 收紧到 −1.5R |

### 10.2 经实证冗余的指标 ❌

| 指标 | 原因 |
|------|------|
| **BB 中轨上方** | 与 MA stack 重叠（v6 验证） |
| **BB %B > 0.7** | 即使换成 %B 也仍冗余（v7 验证） |
| **BB Squeeze** | 与 ATR 扩张过滤功能重叠 |

**核心结论**：纯趋势策略下，**Bollinger Bands 在任何形式下都可以删掉**。其信息已被 MA stack + 颈线突破完全覆盖。

### 10.3 各市场最优 filter 总表

```
美股最优 (PF 2.44):
  filters = "macd, obv, mfi, atr_exp, cci"

港股最优 (PF 1.13):
  filters = "atr_exp, kdj"

A股最优 (PF 0.68，含 CN master switch):
  filters = "atr_exp, kdj, mfi"

全市场一刀切 (PF 1.49):
  filters = "cci, kdj, macd, obv"
```

---

## 十一、关键实证发现（最重要的认知）

### 11.1 单笔胜率 33–37% 是常态，盈亏比 > 1.5 才是赚钱来源

所有 8 套变体中胜率从未突破 41%，但 PF 从 v1 的 0.85 提升到 v8 的 1.49+。书中"6 成胜率"目标在 2 级别 + 6 个月 / 3 年数据上不可达。

### 11.2 ⚠️ 分析师纯打分筛选会让收益反向

v3 随机选股 +505% → v3 + 分析师纯打分筛选 **−490%**（落差近 1000 个百分点）

根本原因：分析师"目标价 / 升值空间"打分本质是【均值回归预期】，跟趋势跟随策略相位完全相反。

**正确做法**：分析师权重压到 30% 以下（`rec=0.15 > upside=0.05`），主体用动量 / MA200 强度。

### 11.3 BB 在趋势策略下完全冗余 — v6/v7/v8 三次验证

v6（中轨）/ v7（%B）/ v8（删除）三次实证 BB 不带来新信息。结构性原因：现有 MA stack + 颈线突破已经"代表"了 BB 的同等含义。

### 11.4 周期决定指标用法 — RSI 在 daily 害、hourly 救场

同一指标在不同时间尺度上的统计意义不同。RSI 14 在日线上是"慢指标"，过滤掉很多真信号；在小时线上是"快指标"，过滤极值能避免追高杀跌。

**实操**：日线策略不要加 RSI 过滤；小时线策略加 RSI + MACD 双过滤。

### 11.5 ⚠️ 指标过滤 vs 票池筛选 — 二者不能简单叠加

```
                  无 screen      + screen
─────────────────────────────────────────────
v4 (无指标过滤)     +472%         +677%   ← 票池筛选放大 +205%
v8 (全指标过滤)     +411%         +486%   ← 票池筛选放大 +75%
                    -61           -191
                                  ↑
                    指标过滤"挤掉"了票池筛选的增益
```

为什么？票池筛选选出的是"已经在涨的强势票"，MFI/KDJ/CCI 等指标自然达标。再叠加指标过滤 = 只选"强势票里的最强" → 笔数大减但单笔质量无明显提升。

**实操选择**：
- 重收益（追求 +677%）→ 用 **v4 + screen**（不开额外指标过滤）
- 重风控（追求 PF 2.44）→ 用 **v8 + 全套指标**（不开 screen）

### 11.6 优化的边际收益顺序

```
策略调参（v1 → v3）:           +850 个百分点（最大权重）
v4 多周期+master switch:       小时线 +400+
v6/v7/v8 多指标过滤:           +25~+40 daily 或 PF +0.5
v8 CN master switch:           A 股 -24% → -12%（亏损减半）
正确的票池筛选:                +175~+450 个百分点
错的票池筛选:                  -995 个百分点（毁灭性）
```

**结论：先选战场（市场 / 票池 / 周期），再选战术（策略），最后调指标。**

### 11.7 R-multiple 是真正的衡量标准

PF 和 net 都会被极端票影响，**R-multiple（每笔 / 初始风险）才是策略本质质量**。
v4+screen 单笔均 +0.80R 是顶级水准；v8 单笔均 +0.52R 是因为"票池筛选 + 多指标"挤压。

---

## 十二、推荐执行流程（按目标）

### 12.1 中长线主力配置（推荐 90% 资金用此）

```
策略 = v4
数据 = 3 年日线
票池 = 分析师 + 动量综合筛选 (rec=0.20, upside=0.10, mom 重) top 20 / 市场
止损 = 2.2×ATR 初始 + 2.5×ATR 拖尾 + 10% 硬止损（自动）
加仓 = 浮盈 ≥ 1.5×ATR 后加 1 仓
预期 = 美股 +400% / 港股 +50% / A 股根据 master switch 决定
```

```bash
python3 kline_backtest.py --mode long --strategy v4 --screen --detail
```

### 12.2 风控偏好配置（10% 资金，验证最佳 PF）

```
策略 = v8 (按市场不同 filter)
美股 filters = macd, obv, mfi, atr_exp, cci
港股 filters = atr_exp, kdj
A 股 filters = atr_exp, kdj, mfi (含 CN master)
预期 = PF 2.4+ / 净盈亏低于 12.1 但更稳定
```

```bash
python3 kline_backtest.py --mode long --strategy v8 --filters macd,obv,mfi,atr_exp,cci  # 美股
python3 kline_backtest.py --mode long --strategy v8 --filters atr_exp,kdj                # 港股
python3 kline_backtest.py --mode long --strategy v8 --filters atr_exp,kdj,mfi            # A 股
```

### 12.3 不要这样配（实证亏损路径）

| 配置 | 历史表现 | 不建议原因 |
|------|---------|-----------|
| v3 + 分析师纯打分筛选 | **−490%** | 信号-策略错配 |
| v2/v3 / 小时线 | **−519% ~ −580%** | 小时线必须 v4+ 才能转正 |
| v5 / 小时线（RSI MR）| −106% | RSI MR 模块拖累 |
| v8 在 A 股、不开 master switch | −78% | A 股大熊期必须屏蔽 |

---

## 十三、配套文件与命令

| 文件 | 用途 |
|------|------|
| `kline_backtest.py` | 主程序（含 v1/v2/v3/v4/v5/v6/v7/v8 八套策略 + 筛选器 + 网格搜索） |
| `analysis.txt` | 完整回测分析报告（每代变体、每个指标、每市场详细数据） |
| `data_cache/` | CSV 价格缓存 + 指数缓存 + 分析师 JSON 快照 |

**核心命令**：

```bash
# 历史最优组合（中长线主力）
python3 kline_backtest.py --mode long --strategy v4 --screen --detail

# 各市场最优指标组合
python3 kline_backtest.py --mode long --strategy v8 --filters macd,obv,mfi,atr_exp,cci  # US PF 2.44
python3 kline_backtest.py --mode long --strategy v8 --filters atr_exp,kdj                # HK 最优
python3 kline_backtest.py --mode long --strategy v8 --filters atr_exp,kdj,mfi            # CN 最优

# 网格搜索（128 组合，按 PF 排序）
python3 kline_backtest.py --mode long --strategy v8 --grid

# 完整代际对比
python3 kline_backtest.py --mode long --compare
```

---

## 十四、最重要的一句话（写完所有回测后的总结）

**先选战场（市场 / 票池 / 周期），再选战术（策略），最后调指标。**

策略和筛选必须 edge type 匹配 —— 趋势策略要动量信号（MA / MACD / OBV / MFI），均值回归策略才用分析师目标价。把两者搞混，再好的执行也救不了。

**没有"通吃"的最优**，只有"按目标选"。重收益用 v4+screen（+677%），重风控用 v8+全套指标（PF 2.44）。两者无法叠加。
